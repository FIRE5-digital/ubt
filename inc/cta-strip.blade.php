<section class="cta-strip">
    <div class="ctr">
        <h1>{!! @$page->content->global->cta_heading !!}</h1>
        <div class="cta-links">
            <a href="tel:{!! @$page->content->global->telephone !!}" class="call">{!! @$page->content->global->telephone !!}</a>
            <a href="mailto:{!! @$page->content->global->email !!} "class="email">{!! @$page->content->global->email !!}</a>
        </div>
    </div>
</section>